/// @description Insert description here
// You can write your code in this editor

if((x > (other.x - (other.sprite_width / 2))) 
	and (x < (other.x + (other.sprite_width / 2)))
	and (y > other.y)) {
	x = xstart;
	y = ystart;
}